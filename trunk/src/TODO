
* ShLocalScope: add tempvar facility. Modules have a local scope additionally,
  for holding tempvars. (How?)

* assignments

* don't increment refcounts when loading onto the stack: requires temp-var
  implementation

* init/finalization of non-POD vectors

* a lot of TODO's in the sources
