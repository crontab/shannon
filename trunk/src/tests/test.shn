def byte = sub 0..255
def type nums = enum one, two, three
def intvec = int[]
def charset = char[..]^
def type charset2 = void[char]
def CharIntMap = int[char]
def charfifo = char<>

def n0 = two
def s0 = 'abc'
def i0 = 2 * 3 + 4
def v1 = 'a' | 'b'
def v2 = 'abc' | 'def' | 'g'

// var a = [1, 2, 3]
// var b = {3, 2, 1}
// var c = {'a' = 1, 'b' = 2}

