
* init/finalization of non-POD vectors

* stoQuad storage model

* vector item selection, only R-value to start with

* is, as (sometimes can be evaluated at compile time)

* a lot of TODO's in the sources

